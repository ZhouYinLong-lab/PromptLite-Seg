\begin{document}
\maketitle

\begin{abstract}
Evaluating zero-shot prompted segmentation under uncertain input is difficult because point and box perturbations have no directly comparable scale. We study prompt robustness on all 1,449 PASCAL VOC 2012 validation images, using a disjoint 100-image training subset to calibrate both channels to matched observable quality. Center Color, GrabCut, and superpixel pipelines serve as interpretable CPU baselines alongside SAM ViT-B. Three findings emerge. First, Robust Superpixel improves over Center Color by 0.0640 IoU but remains below GrabCut, locating its value in transparent component analysis rather than state-of-the-art accuracy. Second, SAM's score-based candidate selection provides a modest 0.0193 IoU gain over a single noisy prompt. Third, matched-quality box perturbations become increasingly more damaging than point perturbations as prompt quality decreases, while the channels are indistinguishable near quality 0.9. Stratified results further expose systematic failures on thin structures, small targets, and full-image boxes. The study provides a reproducible robustness benchmark, component ablations, and practical guidance for choosing prompt channels and segmentation methods.
\end{abstract}

\section{Introduction}

Interactive segmentation converts sparse user input into an object mask. Extreme points, iterative clicks, boxes, and foundation-model prompts have progressively reduced the inference-time supervision requirement \cite{dextr,ritm,simpleclick,sam}. Segment Anything (SAM) established a general point-and-box interface with strong zero-shot transfer \cite{sam}.

Prompt quality is nevertheless structured. A displaced point may remain inside an object, whereas a shifted box changes localization and spatial support. Equal numeric noise scales do not imply equal prompt difficulty. Prior work compares boxes, centroids, and random points for SAM \cite{gaus}, adapts with perturbed prompts \cite{ppsam}, estimates SAM uncertainty \cite{uncertainsam}, and measures the gap between simulated and human interactions \cite{benenson}. We therefore measure the realized quality of each channel instead of assuming equal perturbation strength.

The paper is therefore organized around robustness evaluation, with Center Color, GrabCut, and the superpixel pipelines used as interpretable probes rather than as claims of a fundamentally new segmentation architecture. We make three contributions: (1) a hash-pinned train/validation protocol that calibrates point and box perturbations to matched observable quality; (2) a full-validation study of three pre-specified questions with paired inference and family-wise error control; and (3) component, class, target-size, and failure-case analyses that translate aggregate scores into practical method-selection guidance.

\section{Related Work}

\paragraph{Interactive segmentation.}
GrabCut combines graph cuts and iterative appearance models \cite{grabcut}. DEXTR conditions segmentation on extreme points \cite{dextr}; RITM and SimpleClick improve iterative click-based segmentation across datasets \cite{ritm,simpleclick}. Human studies show that simulated interactions do not automatically reproduce annotation behavior \cite{benenson}.

\paragraph{Promptable foundation models.}
SAM supports point, box, and mask prompts \cite{sam}. SAM 2 extends this to streaming video with a memory-based architecture and improved speed \cite{sam2}. EfficientSAM leverages SAM-guided masked-image pretraining to produce a lightweight ViT encoder that retains most zero-shot performance at a fraction of the cost \cite{efficientsam}. HQ-SAM introduces a high-quality output token and global-local feature fusion, improving boundary precision on fine structures without retraining the original encoder \cite{hqsam}. Variational prompting evaluations report that boxes can be stronger than centroid or random points \cite{gaus}. PP-SAM studies perturbed-prompt adaptation \cite{ppsam}; UncertainSAM formalizes uncertainty estimation \cite{uncertainsam}; recent work also optimizes point prompts using learned agents \cite{attackdefense}. BREPS provides the first systematic benchmark of bounding-box robustness in promptable segmentation, finding up to 30\% IoU variation across human annotators \cite{breps}. These results establish prompt robustness and model efficiency as active topics. Our narrower contribution is matched-quality full-validation evidence with pre-specified paired inference and a transparent lightweight method with component ablations.

\paragraph{Transparent baselines.}
SLIC provides efficient, spatially regular Lab-space primitives \cite{slic}. We use SLIC as a representation rather than claim it as a new learned method. Comparison with GrabCut and explicit ablations prevents method complexity from being mistaken for superiority.

\section{Methods}

\subsection{Task and prompts}
For each VOC image, the largest connected foreground component across semantic labels 1--20 defines one binary target. The clean box is its tight rectangle; the clean point maximizes the Euclidean distance transform inside the target. These prompts use ground-truth geometry and define a controlled benchmark, not a model of unaided human annotation. No method is trained or fine-tuned on VOC validation.

\subsection{CPU methods}
Center Color estimates an RGB prototype around the point, thresholds color distance within the box, and applies morphology and point-connected component selection. Robust Superpixel computes SLIC labels and mean Lab/spatial features, estimates point-induced foreground and box-induced background prototypes, combines relative prototype distance with a spatial prior, unions the result with Center Color, and applies the same cleanup. Ablations remove the color seed, spatial prior, or three-box consensus.

GrabCut uses only the prompt: outside-box pixels initialize background, inside-box pixels probable foreground, and a point disk sure foreground. Five iterations are used. Eight OpenCV initialization failures are retained and scored as zero.

\paragraph{Adaptive Superpixel.}
The original Robust Superpixel uses a fixed SLIC segment count of 280 across all images. Adaptive Superpixel replaces this with an image-size-aware count: $n_{\text{seg}} = \max(80, \min(500, \lfloor\sqrt{HW} \cdot 2.0\rfloor))$. Larger images thus receive finer superpixel resolution, while smaller images use fewer, more coherent segments. This preserves a more consistent image-scale granularity across varying resolutions. All other components (color seed, spatial prior, box consensus) are identical to Robust Superpixel, making this a clean one-factor ablation. Target-area stratification is reported only as a secondary diagnostic because target area is not an input to the schedule.

\subsection{SAM and candidate selection}
SAM ViT-B is evaluated with point-only, box-only, and point-plus-box prompts. Under noisy point-plus-box input, five extra prompts are sampled locally around the observed prompt without access to the clean prompt. We compare the single noisy prediction, maximum SAM score, mask-IoU consistency medoid, majority vote, and Oracle best-of-six. Oracle uses target IoU and is a descriptive upper bound only.

\section{Evaluation Protocol}

\subsection{Data separation}
The tuning split contains 100 VOC train images: five largest-component targets per class, sampled with seed 20260719. The evaluation split contains all 1,449 VOC validation rows. The earlier 30-image analysis motivated the hypotheses; because the final evaluation uses the complete validation split, those images necessarily reappear and the two stages are not fully dataset-disjoint. Data manifests contain identifiers and geometry but no image or mask payloads; manifests, sources, algorithm files, SAM source, and the ViT-B checkpoint are SHA-256 pinned.

\subsection{Prompt calibration}
On tuning data only, independent deterministic grid searches match point hit rate and box IoU to the same quality targets using 20 trials per target.

\begin{table}[ht]
\centering
\caption{Tuning-only noise calibration.}
\begin{tabular}{lrrrrr}
\toprule
Severity & Target & Point scale & Hit rate & Box scale & Box IoU \\
\midrule
Mild & 0.85 & 0.1675 & 0.8480 & 0.0400 & 0.8538 \\
Moderate & 0.65 & 0.2725 & 0.6495 & 0.1150 & 0.6509 \\
\bottomrule
\end{tabular}
\end{table}

On validation, moderate point hit rate is 0.6391 and box IoU is 0.6514. This difference is reported without post-hoc recalibration.

\subsection{Hypotheses and inference}
The primary metric is sample-macro IoU. Five SAM trials are averaged within each sample. H1 states Robust Superpixel exceeds Center Color. H2 states SAM score selection exceeds a single moderate noisy prompt. H3 states matched-quality box noise causes a larger loss than point noise, equivalently point-noise IoU minus box-noise IoU is positive.

Each paired effect uses a 20,000-replicate bootstrap interval and a 50,000-permutation sign-flip test. H1--H3 are corrected together by Holm's method at family-wise $\alpha=0.05$. Dice, resource use, strata, alternative selectors, and Oracle are secondary or descriptive.

\section{Results}

\subsection{CPU baselines and ablations}

\begin{table}[ht]
\centering
\caption{Complete VOC validation CPU results. Failures are scored as zero.}
\begin{tabular}{lrrrr}
\toprule
Method & IoU & Dice & Median ms (all) & Failures \\
\midrule
Center Color & 0.5404 & 0.6753 & 13.1 & 0 \\
GrabCut point+box & \textbf{0.6859} & \textbf{0.7751} & 416.8 & 8 \\
Robust Superpixel & 0.6044 & 0.7345 & 199.8 & 0 \\
Adaptive Superpixel & \textbf{0.6117} & \textbf{0.7410} & 199.3 & 0 \\
\quad no color seed & 0.4633 & 0.5977 & 187.0 & 0 \\
\quad no spatial prior & 0.5827 & 0.7152 & 197.6 & 0 \\
\quad single box & 0.6043 & 0.7344 & 188.5 & 0 \\
\bottomrule
\end{tabular}
\end{table}

H1 is supported: Robust Superpixel improves over Center Color by 0.0640 IoU, 95\% CI [0.0585, 0.0696], Holm $p=0.000060$. GrabCut is stronger. Removing the color seed costs 0.1411 IoU and removing the spatial prior costs 0.0217. Removing multi-box consensus changes mean IoU by only $-0.00004$, contradicting the exploratory intuition that box voting was important.

\subsection{Adaptive superpixel resolution}

\begin{table}[ht]
\centering
\caption{Secondary comparison of adaptive and fixed SLIC segment counts on VOC validation.}
\begin{tabular}{lrrrc}
\toprule
Method & Mean IoU & Mean Dice & Med.\ latency & $\Delta$ IoU \\
\midrule
Robust Superpixel (fixed 280) & 0.6044 & 0.7345 & 199.8 ms & --- \\
Adaptive Superpixel & \textbf{0.6117} & \textbf{0.7410} & 199.3 ms & \textbf{+0.0074} \\
\bottomrule
\end{tabular}
\end{table}

This secondary analysis scales the SLIC segment count between 80 and 500 proportionally to $\sqrt{HW}$. The paired mean IoU improvement is $+0.0074$ with 95\% bootstrap CI $[+0.0053, +0.0096]$. Per-area-quartile deltas are $+0.0145$ (Q1, smallest), $+0.0059$ (Q2), $+0.0054$ (Q3), and $+0.0037$ (Q4, largest). The effect is modest---less than the colour-seed or spatial-prior contributions---and adds no model fit or inference call. Because the schedule observes image dimensions rather than target area, the quartile pattern is descriptive evidence rather than a target-size mechanism.

\subsection{Stratified analysis}

Table~\ref{tab:stratified} reports representative per-class IoU. Performance is bimodal: structurally simple, large, box-shaped classes (tvmonitor 0.724, sofa 0.720, bus 0.702) exceed 0.70~IoU, while thin-structured classes (bicycle 0.203, motorbike 0.548) fall far below the dataset mean. The nine bicycle samples among the worst 20---23.7\% of all bicycles versus 1.3\% for cars---confirm that superpixel-based methods fail systematically on wire-like geometries where even the tight bounding box contains substantial background. Figure~\ref{fig:perclass} in Appendix~\ref{app:extended} gives the complete all-method comparison.

\begin{table}[ht]
\centering
\caption{Per-class IoU for selected classes (full Robust Superpixel).}
\label{tab:stratified}
\begin{tabular}{lrlr}
\toprule
Best classes & IoU & Worst classes & IoU \\
\midrule
tvmonitor & 0.7237 & bicycle & 0.2032 \\
sofa & 0.7195 & aeroplane & 0.5333 \\
bus & 0.7023 & motorbike & 0.5479 \\
cat & 0.6704 & chair & 0.5488 \\
dog & 0.6169 & bird & 0.5916 \\
\midrule
\multicolumn{2}{l}{Sample-weighted mean (20 classes)} & \multicolumn{2}{r}{0.6044} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Effect of target area.}
All methods improve on larger targets. The largest area quartile exceeds the smallest by 0.064~IoU (Center Color), 0.210 (GrabCut), and 0.118 (Robust Superpixel). The colour-seed ablation shows the steepest area dependence ($+0.253$ IoU from Q1 to Q4), indicating that colour-based segmentation is most fragile on small regions where reliable prototypes are harder to estimate. GrabCut's lower mean performance on small targets is consistent with less stable appearance estimation from fewer foreground pixels (Figure~\ref{fig:perarea} in Appendix~\ref{app:extended}); this aggregate degradation is distinct from its eight hard \texttt{initGMMs} failures, which are caused by full-image boxes leaving no definite-background pixels.

\paragraph{Failure cases.}
The 20 worst Robust Superpixel predictions (IoU $<$ 0.13) share three traits: (i)~9 of 20 are bicycles, (ii)~median target area is 7,933~px vs.\ 27,023~px dataset-wide, and (iii)~three targets below 500~px produce near-zero IoU. On these difficult samples, Center Color (0.14~IoU) and GrabCut (0.30~IoU) both outperform Robust Superpixel (0.07), suggesting that the superpixel pipeline's additional structure can be counterproductive when the underlying SLIC segmentation is itself unreliable on thin or tiny objects. Conversely, the best 20 predictions are dominated by large, uniformly coloured objects (sofas, cats, monitors) with median area 35,687~px. Appendix Figure~\ref{fig:failure} visualizes representative extremes.

\subsection{SAM modality}

\begin{table}[ht]
\centering
\caption{Clean SAM ViT-B prompt modalities.}
\begin{tabular}{lrr}
\toprule
Prompt & IoU & Dice \\
\midrule
Point only & 0.5389 & 0.6235 \\
Box only & 0.8479 & 0.9058 \\
Point+box & \textbf{0.8491} & \textbf{0.9087} \\
\bottomrule
\end{tabular}
\end{table}

Point+box is only 0.0012 IoU above box-only. The full-validation result refines the exploratory claim: SAM is box-dominated, but box-only is not stronger in aggregate.

Figure~\ref{fig:samvscpu} compares SAM and CPU methods on six diagnostic classes chosen to expose thin-structure, rigid-object, and category-dependent behavior. SAM point-only underperforms the simple Center Color baseline on several rigid-object classes (tvmonitor: 0.518 vs.\ 0.668; sofa: 0.411 vs.\ 0.670; bus: 0.336 vs.\ 0.577). Under this protocol, the distance-transform point identifies a safe interior location but supplies no object extent. SAM box-only, by contrast, dominates all CPU methods on almost every selected class (0.924 on tvmonitor, 0.947 on bus), showing that the tight box is much more informative than a single interior point for SAM ViT-B in this benchmark. On bicycle, even SAM box-only reaches only 0.356, consistent with thin structures and substantial background inside the tight box; the experiment does not isolate a dataset or architecture ceiling.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{per_class_key_sam_vs_cpu.pdf}
\caption{Mean IoU on six diagnostic classes. SAM point-only underperforms CPU methods on rigid, box-shaped classes; SAM box-only is strongest on all selected classes except bicycle.}
\label{fig:samvscpu}
\end{figure}

\subsection{GrabCut failure investigation}

The eight GrabCut failures (0.55\% of validation samples) share an exact geometric condition: every tight box spans the full image. Our mask initializer labels only pixels outside the box as definite background, so these cases supply no background samples and OpenCV raises the \texttt{initGMMs} assertion. Their large median target area (144,517~px vs.\ 27,023~px dataset-wide) and sofa frequency (3 of 8) are accompanying characteristics, not the established cause. All eight failures are conservatively scored as zero IoU in aggregate metrics. A deployable implementation should detect a full-image box and use a fallback such as Center Color or an inset/background-border initialization.

\subsection{Primary hypotheses}

\begin{table}[ht]
\centering
\caption{Pre-specified paired IoU effects with Holm correction.}
\begin{tabular}{p{0.46\linewidth}rrr}
\toprule
Comparison & Mean $\Delta$ & 95\% CI & Holm $p$ \\
\midrule
H1: Robust Superpixel $-$ Center Color & 0.0640 & [0.0585, 0.0696] & 0.000060 \\
H2: score selection $-$ single noisy & 0.0193 & [0.0135, 0.0251] & 0.000060 \\
H3: point-noise IoU $-$ box-noise IoU & 0.1069 & [0.0997, 0.1138] & 0.000060 \\
\bottomrule
\end{tabular}
\end{table}

Moderate point noise yields 0.8194 IoU and matched-quality box noise 0.7125, supporting H3. Under joint noise, a single prompt reaches 0.6529, score selection 0.6723, consistency medoid 0.6758, vote consensus 0.6710, and Oracle 0.7822. H2 is statistically supported but modest. Consistency medoid was not a primary comparison and remains exploratory; Oracle is not deployable.

\paragraph{Multi-quality sensitivity.}
In a secondary analysis, tuning-only calibration targets observable qualities 0.9, 0.8, 0.7, 0.6, and 0.5; all 1,449 validation samples are then evaluated with 20 trials per target and channel (289,800 SAM inferences). Point-noise minus box-noise IoU is $-0.0016$ [${-0.0041}$, 0.0009] at 0.9, then 0.0238 [0.0201, 0.0275], 0.0756 [0.0703, 0.0809], 0.1497 [0.1430, 0.1563], and 0.2324 [0.2246, 0.2402]. Thus H3's direction is stable from 0.8 to 0.5 but not distinguishable at near-clean quality. Matching hit rate to box IoU remains an engineering calibration, not perceptual equivalence.

\subsection{Practical method selection}

Table~\ref{tab:guidance} distills the experimental evidence into actionable recommendations. The choice of method should depend on the target-object profile and available resources, not on aggregate benchmark ranking alone.

\begin{table}[ht]
\centering
\caption{Method selection guide based on target characteristics and constraints.}
\label{tab:guidance}
\begin{tabular}{p{0.28\linewidth}p{0.62\linewidth}}
\toprule
Scenario & Recommended method (and why) \\
\midrule
GPU available, box prompt feasible & SAM box-only (0.848 IoU; box dominates; point adds $<$0.002) \\
GPU available, point-only only & SAM point-only is competitive for cats/cows/birds (0.72--0.81); Center Color is stronger on rigid objects (0.58--0.67 vs.\ SAM 0.34--0.52) \\
CPU only, accuracy priority & GrabCut point+box (0.686 IoU); full-image boxes cause 8/1449 initialization failures---fall back to Center Color \\
CPU only, speed priority & Center Color (13 ms, 0.540 IoU) or Adaptive Superpixel (199 ms, 0.612 IoU) \\
Bicycles (thin/wire-like) & SAM box-only is the only method exceeding 0.30 IoU; all CPU methods remain below 0.21 \\
Large rigid objects (tv, sofa, bus) & GrabCut or SAM box-only; even Center Color reaches 0.58--0.67 \\
Small targets ($<12$K px) & Adaptive Superpixel (+0.0145 over fixed, secondary); avoid GrabCut colour-seed ablation (0.28 IoU) \\
\bottomrule
\end{tabular}
\end{table}

\noindent A secondary ADE20K evaluation uses a deterministic 200-sample subset selected after scanning all 2,000 validation rows; Appendix~\ref{app:ade20k} reports its scope and results.

\section{Discussion}

Representative evaluation changes the original interpretation. The proposed CPU method is reliably better than its minimal baseline, but not GrabCut; its value lies in transparency and a speed--accuracy operating point. Box localization remains SAM's principal signal, consistent with prior prompting studies \cite{gaus} and BREPS \cite{breps}; our added evidence is that its greater sensitivity emerges progressively as matched observable quality degrades, while the channels are indistinguishable near 0.9. SAM score selection becomes statistically reliable at full scale, although the improvement is only 0.0193 IoU and requires extra decoder calls.

Stratified analysis reveals systematic weaknesses: bicycles and other thin-structured objects consistently fail (IoU $<$ 0.21) because superpixel segmentation cannot capture wire-like geometry, and small targets (lowest area quartile) suffer across all CPU methods. The colour-seed component is the most area-sensitive ($+0.25$ IoU Q4$-$Q1), confirming that colour prototypes degrade on small regions. These per-class and per-size patterns---not just aggregate scores---should guide users in selecting the appropriate method for their target-object characteristics.

Negative results are equally informative. Multi-box consensus adds complexity without aggregate benefit. The strongest CPU method is classical GrabCut. On the hardest 20 samples, all CPU methods fail, but Center Color (0.14) and GrabCut (0.30) are more robust than the superpixel-based method (0.07), suggesting that structural priors can become liabilities when the underlying image segmentation is unreliable.

\section{Threats to Validity}

Ground-truth-derived prompts are more controlled than real user input. Numeric matching of hit rate and box IoU does not make the metrics psychologically equivalent, and neither distribution is fitted to human errors. A privacy-minimizing human-prompt pilot protocol and collection tool are provided, but no participants were recruited and no human result is claimed. The benchmark selects one largest component per image rather than every instance. VOC 2012 and SAM ViT-B are the only full-scale model evaluation. The ADE20K study deterministically samples 200 of 2,000 validation rows and evaluates CPU methods only; it improves transfer coverage but is not a complete second-dataset confirmation. Full ADE20K evaluation, larger checkpoints, and learned interactive baselines would improve external validity. The earlier exploratory 30 images reappear inside the complete validation split (2.1\%), so H1--H3 are full-scale but not independently preregistered on a disjoint dataset. Adaptive Superpixel and the five-target curve are secondary analyses, and the image-size schedule does not identify a target-size mechanism.

\section{Conclusion}

Across all 1,449 VOC validation images, Robust Superpixel improves over Center Color but trails GrabCut; a secondary adaptive-resolution comparison yields a small paired gain; and SAM's box channel dominates, with point-only underperforming CPU baselines on several rigid classes. Matched-quality box noise causes substantially larger loss than point noise in H3; a five-target curve localizes this asymmetry to quality 0.8 and below, with no distinguishable difference near 0.9. SAM score selection offers a modest but Holm-significant gain. Bicycles and small targets are systematic failure modes, while GrabCut's eight hard failures arise from full-image boxes. Together, these results provide a reproducible, stratified account of prompt-channel robustness and practical trade-offs.

\clearpage
\fontsize{8}{9}\selectfont
\begin{thebibliography}{99}
\bibitem{voc} M. Everingham et al. The PASCAL Visual Object Classes Challenge. \emph{IJCV}, 2010. \url{https://www.robots.ox.ac.uk/~vgg/projects/pascal/VOC/}
\bibitem{grabcut} C. Rother, V. Kolmogorov, and A. Blake. GrabCut: Interactive Foreground Extraction Using Iterated Graph Cuts. \emph{ACM TOG}, 2004.
\bibitem{slic} R. Achanta et al. SLIC Superpixels Compared to State-of-the-Art Superpixel Methods. \emph{TPAMI}, 2012.
\bibitem{dextr} K.-K. Maninis et al. Deep Extreme Cut: From Extreme Points to Object Segmentation. \emph{CVPR}, 2018.
\bibitem{ritm} K. Sofiiuk, I. Petrov, and A. Konushin. Reviving Iterative Training with Mask Guidance for Interactive Segmentation. \emph{ICIP}, 2022.
\bibitem{simpleclick} Q. Liu et al. SimpleClick: Interactive Image Segmentation with Simple Vision Transformers. \emph{ICCV}, 2023.
\bibitem{sam} A. Kirillov et al. Segment Anything. \emph{ICCV}, 2023.
\bibitem{gaus} Y. Gaus et al. Performance Evaluation of Segment Anything Model with Variational Prompting for Application to Non-Visible Spectrum Imagery. \emph{CVPRW}, 2024.
\bibitem{ppsam} A. Rahman et al. PP-SAM: Perturbed Prompts for Robust Adaption of Segment Anything Model for Polyp Segmentation. \emph{CVPRW}, 2024.
\bibitem{uncertainsam} T. Kaiser et al. UncertainSAM: Fast and Efficient Uncertainty Quantification of the Segment Anything Model. \emph{ICML}, 2025.
\bibitem{benenson} R. Benenson, S. Popov, and V. Ferrari. Large-Scale Interactive Object Segmentation with Human Annotators. \emph{CVPR}, 2019.
\bibitem{attackdefense} Liu et al. Attack for Defense: Adversarial Agents for Point Prompt Optimization Empowering Segment Anything Model. \emph{CVPR}, 2026.
\bibitem{ade20k} B. Zhou et al. Scene Parsing through ADE20K Dataset. \emph{CVPR}, 2017. \url{https://groups.csail.mit.edu/vision/datasets/ADE20K/}
\bibitem{sam2} N. Ravi et al. SAM 2: Segment Anything in Images and Videos. \emph{arXiv:2408.00714}, 2024.
\bibitem{efficientsam} Y. Xiong et al. EfficientSAM: Leveraged Masked Image Pretraining for Efficient Segment Anything. \emph{CVPR}, 2024.
\bibitem{hqsam} L. Ke et al. Segment Anything in High Quality. \emph{NeurIPS}, 2023.
\bibitem{breps} A. Moskalenko et al. BREPS: Bounding-Box Robustness Evaluation of Promptable Segmentation. \emph{AAAI}, 2026.
\end{thebibliography}

\clearpage
\normalsize
\appendix

\section{Extended stratified analysis}
\label{app:extended}

The main paper reports the numerical conclusions required for the hypothesis tests. This appendix provides the corresponding full-resolution diagnostic plots.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{per_class_cpu_split.pdf}
\caption{Per-class mean IoU for all CPU methods on VOC 2012 validation, split into two 10-class panels for readability. Bicycle is systematically the most difficult class (all methods $<$ 0.21 IoU).}
\label{fig:perclass}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\linewidth]{per_area_iou.png}
\caption{Mean IoU by target area quartile. All methods improve with area; the colour-seed ablation (Robust $-$color) shows the steepest climb, reflecting unreliable colour prototypes on small regions.}
\label{fig:perarea}
\end{figure}

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{failure_report_grid.png}
\caption{Error maps for three worst and three best Robust Superpixel predictions. Green = true positive, red = false positive, blue = false negative. Worst cases (top row) are dominated by thin structures and small targets; best cases (bottom row) are large, uniformly coloured objects.}
\label{fig:failure}
\end{figure}

\section{Secondary Evaluation on ADE20K}
\label{app:ade20k}

We scan all 2,000 rows of an ADE20K validation mirror \cite{ade20k}, then use seed 20260720 to select 200 rows without replacement for CPU evaluation. All rows pass the 256-px minimum-area filter. Samples come from the \texttt{1aurent/ADE20K} Hugging Face mirror; targets and clean prompts follow the VOC protocol. The manifest, selected identifiers, per-sample metrics, and hashes are released without images or masks.

\begin{table}[ht]
\centering
\caption{CPU methods on a deterministic 200-sample ADE20K subset.}
\begin{tabular}{lrrr}
\toprule
Method & Mean IoU & Mean Dice & Med.\ latency \\
\midrule
Center Color & 0.6350 & 0.7541 & 23.9 ms \\
GrabCut point+box & \textbf{0.7383} & 0.8169 & 1901.7 ms \\
Robust Superpixel & 0.7150 & 0.8178 & 362.9 ms \\
Adaptive Superpixel & 0.7288 & \textbf{0.8278} & 361.7 ms \\
\quad no color seed & 0.5162 & 0.6567 & 339.2 ms \\
\quad no spatial prior & 0.6819 & 0.7903 & 360.6 ms \\
\quad single box & 0.7159 & 0.8184 & 338.3 ms \\
\bottomrule
\end{tabular}
\end{table}

The IoU ranking is GrabCut $>$ Adaptive Superpixel $>$ Robust Superpixel $>$ Center Color. Removing the color seed costs 0.1989 IoU and removing the spatial prior costs 0.0332, while the single-box variant differs from the multi-box default by only $+0.0008$. Adaptive resolution improves descriptively by 0.0138 IoU over the fixed schedule. These patterns agree with the VOC component analysis, but this 200-sample evaluation does not replace a complete cross-dataset evaluation.

\noindent\textbf{Coverage.} The complete 2,000-row stream was scanned and all rows were eligible; 200 seeded rows (10\%) were evaluated with seven CPU methods, producing 1,400 metric rows with no method failures. The same resume-safe runner supports full evaluation, but the remaining 1,800 rows were not run.

\end{document}
